{
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "lint": "pnpm run compile",
    "compile": "pnpm --filter client exec tsc --noEmit && pnpm --filter server exec tsc --noEmit",
    "build": "pnpm run compile && pnpm --filter client... build && pnpm --filter server... build",
    "dev": "concurrently \"pnpm --filter server dev\" \"pnpm --filter client dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "serverless-http": "^3.2.0"
  }
}
